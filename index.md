---
layout: default
title: zdlab.xyz
---

- Use root-level paths for short links, e.g. `/gh` or `/cv`.
- Add new links by creating files under `_links/` with a `redirect_to` value.

Example:

```yaml
---
redirect_to: https://github.com/zdlab
---
```
